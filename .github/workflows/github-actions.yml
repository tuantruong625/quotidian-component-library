name: quotidian-component-library-ci
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - name: Run Tests
      run: npm test
